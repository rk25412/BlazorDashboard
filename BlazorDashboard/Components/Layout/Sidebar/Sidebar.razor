<aside id="sidebar"
       class="sidebar @(_showSidebar ? "" : "hidden-sidebar")">
    <ul class="sidebar-nav">
        @foreach (var item in _sidebarNavItems)
        {
            <SidebarNavItem NavItem="@item" />
        }
    </ul>
</aside>